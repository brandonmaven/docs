---
title: "Cancel Session"
api: "POST /v1/sessions/{id}/cancel"
description: "Cancel a pending session"
---

Cancel a pending session. Only sessions in `created` or `collecting` status can be cancelled.

## Path Parameters

<ParamField path="id" type="string" required>
  Session UUID
</ParamField>

## Response

Returns `204 No Content` on success.

<RequestExample>

```bash cURL
curl -X POST https://api.trymaven.com/v1/sessions/{session_id}/cancel \
  -H "Authorization: Bearer {api_key}"
```

```javascript Node.js
const response = await fetch(
  'https://api.trymaven.com/v1/sessions/{session_id}/cancel',
  {
    method: 'POST',
    headers: { 'Authorization': 'Bearer {api_key}' }
  }
);

// 204 No Content on success
```

```python Python
import requests

response = requests.post(
    'https://api.trymaven.com/v1/sessions/{session_id}/cancel',
    headers={'Authorization': 'Bearer {api_key}'}
)

# 204 No Content on success
```

</RequestExample>

<ResponseExample>

```text Response
204 No Content
```

</ResponseExample>
